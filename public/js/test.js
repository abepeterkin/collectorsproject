<!-- Ignore me! I'm just a file for Martin's testing pleasure :) -->
$(document).ready(function() {

	//	var canvas = document.getElementById("canvas");
//		var objectImg;
		
/*	function drawObject() {
		$.post("/search/" + object, function(data) {
			console.log(data);
	//		objectImg = data.image;
		});
		objectImg
		canvasContext.drawImage(objectImg, 150,300);
	}
	
	function retrieveLinks() {
		// Retrieve the provenance info and store in string
		// Check the string against values in database tables
		// Return values from database tables including relational info
		// Draw returned values as objects
	}*/
});